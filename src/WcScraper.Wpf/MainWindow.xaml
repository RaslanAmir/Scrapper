
<Window x:Class="WcScraper.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WcScraper.Wpf.ViewModels"
        xmlns:svc="clr-namespace:WcScraper.Wpf.Services"
        mc:Ignorable="d"
        Title="WC Local Scraper (WPF)" Height="580" Width="900">
    <Window.DataContext>
        <vm:MainViewModel>
            <vm:MainViewModel.Dialogs>
                <svc:DialogService />
            </vm:MainViewModel.Dialogs>
        </vm:MainViewModel>
    </Window.DataContext>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- URL + Run -->
        <TextBlock Text="Store URL:" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <TextBox Grid.Column="1" Text="{Binding StoreUrl, UpdateSourceTrigger=PropertyChanged}" MinWidth="520" />
        <Button Grid.Column="2" Content="Run" Margin="8,0,0,0" Padding="16,6" Command="{Binding RunCommand}" />

        <!-- Output -->
        <TextBlock Grid.Row="1" Text="Output folder:" VerticalAlignment="Center" Margin="0,8,8,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="1" Grid.Column="2" Content="Browse…" Margin="8,8,0,0" Padding="12,6" Command="{Binding BrowseCommand}" />

        <!-- Exports -->
        <GroupBox Grid.Row="2" Grid.ColumnSpan="3" Header="Exports" Margin="0,12,0,8">
            <StackPanel Orientation="Horizontal" Margin="8">
                <CheckBox Content="Generic CSV" IsChecked="{Binding ExportCsv}" Margin="0,0,16,0"/>
                <CheckBox Content="Shopify CSV (variants)" IsChecked="{Binding ExportShopify}" Margin="0,0,16,0"/>
                <CheckBox Content="WooCommerce CSV" IsChecked="{Binding ExportWoo}" Margin="0,0,16,0"/>
                <CheckBox Content="Reviews CSV (Store API)" IsChecked="{Binding ExportReviews}" Margin="0,0,16,0"/>
                <CheckBox Content="XLSX" IsChecked="{Binding ExportXlsx}" Margin="0,0,16,0"/>
                <CheckBox Content="JSONL" IsChecked="{Binding ExportJsonl}"/>
            </StackPanel>
        </GroupBox>

        <!-- Filters -->
        <GroupBox Grid.Row="3" Grid.ColumnSpan="3" Header="Filters (optional)">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Categories" FontWeight="Bold" />
                    <ScrollViewer Height="140" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding CategoryChoices}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Tags" FontWeight="Bold" />
                    <ScrollViewer Height="140" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding TagChoices}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Log -->
        <GroupBox Grid.Row="4" Grid.ColumnSpan="3" Header="Log">
            <ListBox ItemsSource="{Binding Logs}" />
        </GroupBox>

        <!-- Status -->
        <StatusBar Grid.Row="5" Grid.ColumnSpan="3" Margin="0,8,0,0">
            <StatusBarItem Content="{Binding Path=Logs.Count, StringFormat=Messages: {0}}" />
            <StatusBarItem>
                <TextBlock Text="{Binding IsRunning, StringFormat=Running: {0}}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
